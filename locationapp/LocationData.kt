package com.example.locationapp

data class LocationData(    // this data class stores the latitude and longitude data location of the user when the user has granted to use the location
    val latitude : Double,
    val longitude : Double
)
